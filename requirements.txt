SpeechRecognition>=3.17.0
pyttsx3==2.90
wikipedia==1.4.0
requests==2.31.0
python-dotenv==1.0.0
# Note: PyAudio is optional. If you need voice input, install it separately:
# On Windows: python -m pip install pipwin && pipwin install pyaudio
# On macOS: brew install portaudio && pip install pyaudio
# On Linux: sudo apt-get install portaudio19-dev && pip install pyaudio
# The assistant works perfectly with text input without PyAudio!
